/*
 * The data model for the extension. Holds all the data pertaining to browser tabs.
 */

define([], function () {
  'use strict';

  return {

    /* This is where we store the data for a given tab id. */
    tabs: {}
  };
})
