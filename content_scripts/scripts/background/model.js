define([], function () {
  'use strict';

  /**
   * The data model for the extension. Holds data pertaining to each browser tab that is being used with the extension.
   */
  return {

    /* This is where we store the data for a given tab id. */
    tabs: {}
  };
})
