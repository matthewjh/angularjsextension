'use strict';

define([
  'main'
  ],
  function (main) {

    describe('main', function () {
      it('should equal "hello"', function () {
        expect(main).toEqual('hello');
      });
    });
});
